const Header = () => {
  return (
    <div>
      <img src="/logo.png" alt="A server surrounded by magic sparkles." />
      <h1>Welcome to this NextJS Course!</h1>
    </div>
  );
};

export default Header;

// I put the components folder outside the app folder, because I prefer to make the app folder used only for routing and it's pages.
